import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    borderBottomWidth: 1,
    borderBottomColor: '#fff',

    marginTop: 16,
    paddingBottom: 8,

    flexDirection: 'row',
    alignItems: 'center'
  },
  title: {
    marginLeft: 8,
    color: '#fff',

    fontSize: 16
  }
})